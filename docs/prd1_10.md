Content is user-generated and unverified.
数标云管 v1.10 — Claude Code 执行指令（帮助与引导模块，精简版 R2）
项目目标
在已完成的 v1.0 ~ v1.9 代码库基础上，新增按场景出现的操作辅助层： 错误解释与下一步建议 / 流程级帮助 / 字段级提示 / 页面帮助抽屉

本版本不做：独立知识库、帮助内容后台管理界面、帮助内容数据库表、
帮助内容查询API、全文搜索帮助站、AI问答机器人。

零、关键语义边界定义（实施前必读，禁止跳过）
0.1 本版本的定位
帮助系统不是独立模块，是"按场景出现的操作辅助层"：

不打扰正常操作
在用户犹豫、填错、报错时立刻给出可执行说明
0.2 帮助内容的存储原则（最关键）
后端帮助内容：backend/core/help_content.py（纯Python字典，不建数据库表）
前端帮助内容：frontend/src/constants/help.js（纯JS对象，不需要后端API）

前端组件直接读 help.js，不经过任何接口。
没有 GET /help/fields、GET /help/pages 等帮助查询接口。
内容随代码版本管理，改内容 = 改静态文件 + 部署，无需其他操作。
0.3 错误响应追加规则
python
# 现有错误格式（不变，禁止修改）：
# {"detail": "需求已冻结，请通过变更单提交", "code": "REQUIREMENT_FROZEN"}
#
# 追加 help 字段后：
# {
#   "detail": "需求已冻结，请通过变更单提交",   ← 不变
#   "code": "REQUIREMENT_FROZEN",               ← 不变
#   "help": {                                   ← 新增，只追加
#     "reason": "该项目已关联报价单且报价已确认，需求进入冻结状态。",
#     "next_steps": ["步骤1", "步骤2"],
#     "doc_anchor": "change-order-flow"
#   }
# }
#
# 规则：
# 1. help 字段只追加，禁止修改 detail 和 code
# 2. 错误码无对应帮助内容时，响应中不包含 help 字段
# 3. HTTP 5xx 不附加 help
0.4 降级原则
帮助内容缺失时（help.js 无对应 key）：
  不显示提示，不报错，不白屏，静默处理。

帮助系统任何部分故障，不得影响主业务功能。
一、前置检查（任意一项失败则停止，将失败原因写入 PROGRESS.md 后退出）
bash
# 1. 确认 v1.0 ~ v1.9 全量测试通过
pytest tests/ -v --tb=short
# 要求：0 FAILED

# 2. 抽查以下接口，确认错误响应包含 detail 和 code 字段：
#    POST /api/v1/quotes/{id}/accept（已接受状态重复操作）
#    PATCH /api/v1/change-orders/{id}/confirm（终态流转）
#    POST /api/v1/projects/{id}/close（条件不满足）
#    若现有错误响应缺少 code 字段，记录到 PROGRESS.md，在簇 A 优先补齐

# 3. 确认 logs/ 目录存在且 RotatingFileHandler 配置正确
# 4. 确认 .env 文件存在且 SECRET_KEY 已配置
前置检查全部通过后，将以下内容写入项目根目录 PROGRESS.md：

markdown
# v1.10 执行进度

## 状态：执行中
## 开始时间：[自动填写当前时间]

| 簇 | 名称 | 状态 | 完成时间 | 通过测试数 |
|----|------|------|----------|------------|
| A | 错误码规范化与帮助内容——后端 | ⏳ | - | - |
| B | 流程级帮助——前端 | ⏳ | - | - |
| C | 字段级提示——前端 | ⏳ | - | - |
| D | 页面帮助抽屉——前端 | ⏳ | - | - |
| E | 错误展示增强——前端 | ⏳ | - | - |
| F | 全局重构 | ⏳ | - | - |

---

## 失败记录模板
## 状态：失败
## 失败步骤：簇 X / 第 N 步
## 失败原因：[具体错误信息]
## 当前已完成项：[已完成的簇列表]
## 下次恢复入口：[从哪一步重新开始]
二、全局约定
python
# 追加到 backend/core/constants.py，禁止修改已有常量

HELP_CONTENT_VERSION = "1.10"
HELP_MAX_NEXT_STEPS = 5          # 每条错误帮助最多显示步骤数
HELP_FIELD_TIP_MAX_LENGTH = 30   # 字段提示最大字符数（中文）
HELP_PAGE_TIP_MAX_ITEMS = 5      # 页面帮助抽屉最多条目数

# 精确定义：
# 1. help 字段只追加，不修改 detail/code
# 2. 无对应帮助内容的错误码不返回 help 字段
# 3. 帮助内容以静态文件存储，不入数据库，不建查询接口
三、执行清单
簇 A：错误码规范化与帮助内容——后端
目标：补齐错误响应 code 字段，建立帮助内容映射表，修改异常处理器追加 help 字段。

步骤 1：新建 backend/core/error_codes.py
python
ERROR_CODES = {
    # 报价模块（v1.6）
    "QUOTE_STATUS_INVALID_TRANSITION": "报价单状态流转不合法",
    "QUOTE_ALREADY_CONVERTED": "报价单已转换为合同，不可重复转换",
    "QUOTE_NOT_ACCEPTED": "仅已接受的报价单可转换合同",

    # 变更冻结（v1.7）
    "REQUIREMENT_FROZEN": "需求已冻结",
    "CHANGE_ORDER_INVALID_TRANSITION": "变更单状态流转不合法",

    # 里程碑收款（v1.7）
    "MILESTONE_NOT_COMPLETED": "里程碑未完成，不可触发收款流转",
    "PAYMENT_STATUS_INVALID_TRANSITION": "收款状态流转不合法",

    # 项目关闭（v1.7）
    "PROJECT_CLOSE_CONDITIONS_NOT_MET": "项目关闭条件未满足",
    "PROJECT_ALREADY_CLOSED": "项目已关闭",

    # 发票（v1.8）
    "INVOICE_AMOUNT_EXCEEDS_CONTRACT": "发票金额累计超过合同金额",
    "INVOICE_STATUS_INVALID_TRANSITION": "发票状态流转不合法",
    "INVOICE_CANNOT_DELETE": "已核销或已作废的发票不可删除",

    # 通用
    "NOT_FOUND": "记录不存在",
    "VALIDATION_ERROR": "输入数据校验失败",
    "DUPLICATE_ENTRY": "数据重复，违反唯一约束",
    "FOREIGN_KEY_VIOLATION": "关联数据不存在或被其他记录引用",
}
步骤 2：新建 backend/core/help_content.py
python
from backend.core.constants import HELP_MAX_NEXT_STEPS

# 纯Python字典，不建数据库表，不建查询接口
HELP_CONTENT = {
    "REQUIREMENT_FROZEN": {
        "reason": "该项目已关联报价单且报价状态为已接受，需求进入冻结状态，不允许直接修改。",
        "next_steps": [
            "前往项目详情页 → 变更单 Tab",
            "点击「新建变更单」，填写变更描述和额外费用",
            "等待变更单状态流转为 confirmed",
            "confirmed 后需求变更才正式生效",
        ],
        "doc_anchor": "change-order-flow",
    },
    "CHANGE_ORDER_INVALID_TRANSITION": {
        "reason": "变更单当前状态不允许执行此操作。confirmed/rejected/cancelled 为终态。",
        "next_steps": [
            "pending 状态可执行：确认、拒绝、撤回",
            "confirmed/rejected/cancelled 为终态，不可再操作",
            "如需重新提交，请新建一条变更单",
        ],
        "doc_anchor": "change-order-status",
    },
    "MILESTONE_NOT_COMPLETED": {
        "reason": "收款状态流转需要里程碑先标记为已完成。",
        "next_steps": [
            "前往项目详情页 → 里程碑列表",
            "找到对应里程碑，将状态更新为「已完成」",
            "完成后即可触发开票/收款流转",
        ],
        "doc_anchor": "milestone-payment",
    },
    "PROJECT_CLOSE_CONDITIONS_NOT_MET": {
        "reason": "项目关闭需要满足所有前置条件，当前有条件未满足。",
        "next_steps": [
            "点击「关闭条件检查」查看具体未满足项",
            "确保所有里程碑状态为已完成",
            "确保存在 acceptance_type=final 且 status=passed 的验收记录",
            "确保所有里程碑收款状态为已到账（或收款金额为0）",
            "确保至少有一条交付物记录",
        ],
        "doc_anchor": "project-close",
    },
    "PROJECT_ALREADY_CLOSED": {
        "reason": "项目已完成关闭，不可重复关闭，核心字段不可修改。",
        "next_steps": [
            "已关闭项目数据仅供查阅",
            "如需查看关闭条件快照，前往项目详情页查看 close_checklist",
        ],
        "doc_anchor": "project-close",
    },
    "INVOICE_AMOUNT_EXCEEDS_CONTRACT": {
        "reason": "同一合同下所有未作废发票的金额合计不得超过合同总金额。",
        "next_steps": [
            "前往合同详情页 → 发票 Tab 查看已开票金额进度条",
            "检查是否有重复开票",
            "如需开票金额超出合同，请先修改合同金额",
        ],
        "doc_anchor": "invoice-amount-limit",
    },
    "INVOICE_STATUS_INVALID_TRANSITION": {
        "reason": "发票状态只能按固定顺序流转：草稿→已开票→客户已收→已核销。",
        "next_steps": [
            "草稿可流转到：已开票、已作废",
            "已开票可流转到：客户已收、已作废",
            "客户已收可流转到：已核销",
            "已核销和已作废为终态，不可再操作",
        ],
        "doc_anchor": "invoice-status",
    },
    "INVOICE_CANNOT_DELETE": {
        "reason": "已核销（verified）和已作废（cancelled）的发票不允许删除，以保证财务记录完整性。",
        "next_steps": [
            "已核销发票如有问题请联系代理记账处理",
            "如需作废已开票发票，可将状态流转为「已作废」",
        ],
        "doc_anchor": "invoice-delete",
    },
    "QUOTE_ALREADY_CONVERTED": {
        "reason": "一张报价单只能转换为一份合同，该报价单已完成转换。",
        "next_steps": [
            "前往报价单详情页查看已关联的合同编号",
            "如需新合同，请重新创建报价单",
        ],
        "doc_anchor": "quote-convert",
    },
    "QUOTE_NOT_ACCEPTED": {
        "reason": "只有状态为「已接受」的报价单才能转换为合同。",
        "next_steps": [
            "先执行「接受」操作，将报价单状态变更为 accepted",
            "再执行「转换合同」",
        ],
        "doc_anchor": "quote-convert",
    },
}


def get_help(error_code: str) -> dict | None:
    """返回错误码对应帮助内容，不存在时返回 None"""
    content = HELP_CONTENT.get(error_code)
    if not content:
        return None
    return {
        "reason": content["reason"],
        "next_steps": content["next_steps"][:HELP_MAX_NEXT_STEPS],
        **({"doc_anchor": content["doc_anchor"]} if "doc_anchor" in content else {}),
    }
步骤 3：修改现有异常处理器（不新建文件）
python
# backend/core/exception_handlers.py（修改现有文件）
from backend.core.help_content import get_help

def build_error_response(detail: str, code: str) -> dict:
    """
    在现有 detail/code 基础上追加 help。
    禁止修改 detail 和 code 的内容。
    """
    response = {"detail": detail, "code": code}
    help_data = get_help(code)
    if help_data:
        response["help"] = help_data
    return response
创建测试文件 tests/test_help_backend.py
python
# test_error_response_includes_help_when_code_matched → NFR-1001
# test_error_response_no_help_when_code_not_in_mapping → NFR-1001
# test_help_next_steps_capped_at_max → NFR-1001
# test_detail_not_modified_by_help_injection → NFR-1001
# test_code_not_modified_by_help_injection → NFR-1001
# test_requirement_frozen_returns_correct_help → NFR-1001
# test_invoice_amount_exceeds_returns_correct_help → NFR-1001
# test_project_close_not_met_returns_correct_help → NFR-1001
# test_milestone_not_completed_returns_correct_help → NFR-1001
# test_5xx_error_does_not_include_help → NFR-1001
# test_get_help_returns_none_for_unknown_code → NFR-1001
运行全量测试，要求 0 FAILED。更新 PROGRESS.md，继续执行簇 B。

簇 B：流程级帮助——前端
目标：实现业务流程总览，帮助用户理解当前处于哪一步。

前端静态数据（写入 frontend/src/constants/help.js）
javascript
// 所有帮助内容集中在此文件，前端组件直接导入，不经过接口

export const WORKFLOW_STEPS = [
  {
    id: "quote", label: "报价", version: "v1.6",
    description: "向客户提供报价，确认工期和费用",
    key_actions: ["创建报价单", "发送给客户", "客户接受后转合同"],
    route: "/quotes",
    triggers_next: "报价状态变为 accepted 时，可触发转合同",
  },
  {
    id: "contract", label: "合同", version: "v1.0",
    description: "正式合同签订，锁定项目范围和金额",
    key_actions: ["从报价单转换", "手动创建", "签订后关联项目"],
    route: "/contracts",
    triggers_next: "合同 active 后可创建关联项目",
  },
  {
    id: "project", label: "项目执行", version: "v1.7",
    description: "跟踪项目进度、里程碑、变更和工时",
    key_actions: ["设置里程碑", "记录工时", "提交变更单", "完成验收"],
    route: "/projects",
    triggers_next: "所有里程碑完成 + 验收通过 + 款项到账后可关闭项目",
  },
  {
    id: "invoice", label: "发票", version: "v1.8",
    description: "开具销项发票，关联合同和收款",
    key_actions: ["里程碑完成后开票", "确认客户收票", "财务核销"],
    route: "/invoices",
    triggers_next: "发票核销后可纳入对账报表",
  },
  {
    id: "finance", label: "收款与对账", version: "v1.8",
    description: "记录实际收款，月末导出数据给代理记账",
    key_actions: ["记录收款", "月末导出Excel", "执行对账同步"],
    route: "/finance",
    triggers_next: "数据导出后交代理记账处理报税",
  },
  {
    id: "risk", label: "风险监控", version: "v1.9",
    description: "监控逾期收款、数据一致性和项目利润",
    key_actions: ["查看逾期预警", "运行数据核查", "查看项目粗利润"],
    route: "/finance/consistency-check",
  },
]
前端展示要求
入口：顶部导航栏新增"业务流程"链接；首页看板新增流程入口卡片
展示：水平步骤条（桌面）/ 垂直列表（移动端）
每个步骤可展开/收起：描述 + 关键操作 + 下一步触发条件 + 跳转链接
当前页面路由对应步骤自动高亮
只读静态展示，不做编辑，不调用任何接口
验收标准
6个步骤全部展示，顺序正确
移动端垂直布局不溢出
当前页面对应步骤高亮
无任何接口调用（纯静态）
运行全量测试，要求 0 FAILED。更新 PROGRESS.md，继续执行簇 C。

簇 C：字段级提示——前端
目标：为关键字段添加 tooltip，hover/聚焦时显示一句话说明，内容来自 help.js。

字段提示内容（追加到 frontend/src/constants/help.js）
javascript
export const FIELD_TIPS = {
  quote: {
    estimate_days: "预计完成项目所需的工作天数，用于计算人工成本。",
    daily_rate: "每个工作日的费率（元/天），乘以工期得出人工费用。",
    risk_buffer_rate: "风险缓冲比例，如填 0.1 表示在基础金额上加 10% 缓冲。",
    valid_until: "报价有效截止日期，过期后状态自动变为已过期。",
    discount_amount: "在小计金额基础上直接减去的折扣金额（元）。",
    tax_rate: "增值税率，如 0.13 表示 13%，用于计算税额。",
  },
  contract: {
    sign_date: "合同正式签订日期，用于会计期间归属计算。",
    amount: "合同总金额，所有发票累计开票额不得超过此金额。",
  },
  invoice: {
    invoice_date: "开票日期，用于会计期间归属，建议与实际开票一致。",
    amount_excluding_tax: "不含税金额，系统自动计算税额和价税合计。",
    tax_rate: "适用税率：一般纳税人 13%，小规模纳税人 3%。",
    received_by: "客户方收票联系人姓名或部门，便于回访确认。",
  },
  milestone: {
    payment_amount: "该里程碑对应的收款金额，完成后才能触发开票流程。",
    payment_due_date: "预计收款截止日期，超期未收款将出现在逾期预警中。",
    payment_status: "unpaid=未付款，invoiced=已开票，received=款项已到账。",
  },
  change_order: {
    extra_days: "此次变更增加的预计工作天数，0 表示非工期变更。",
    extra_amount: "此次变更增加的费用（元），可为 0（非付费变更）。",
    status: "pending=等待客户确认，confirmed=已确认可进入开发，rejected/cancelled=终态。",
  },
  work_hour: {
    hours_spent: "本次实际投入的工作小时数，用于计算项目工时成本。",
    deviation_note: "当实际工时超出预计 20% 时必填，说明超出原因。",
  },
  fixed_cost: {
    period: "monthly=月费，quarterly=季费，yearly=年费，onetime=一次性费用。",
    effective_date: "该成本条目开始生效日期，月度汇总从此日期起统计。",
    end_date: "该成本条目失效日期，留空表示持续有效。",
  },
}
组件规范
新建 frontend/src/components/FieldTip.vue：

Props：module: String，field: String
行为：
  从 FIELD_TIPS[module][field] 读取内容（直接导入 help.js，不调用接口）
  无对应内容时不渲染，静默处理
  桌面：字段 label 旁 ❓ 图标，hover 显示 tooltip
  移动端：placeholder 追加简化说明（截取前15字）
必须覆盖的字段
报价单表单：estimate_days / daily_rate / risk_buffer_rate / valid_until / tax_rate / discount_amount
发票表单：invoice_date / amount_excluding_tax / tax_rate / received_by
里程碑表单：payment_amount / payment_due_date / payment_status
变更单表单：extra_days / extra_amount / status
工时记录表单：hours_spent / deviation_note
固定成本表单：period / effective_date / end_date
验收标准
所有必须字段均渲染 FieldTip 组件
Tooltip 内容不超过 30 个中文字
移动端降级为 placeholder，不显示 tooltip
无对应内容时不渲染，不报错，不白屏
无任何接口调用（纯静态）
运行全量测试，要求 0 FAILED。更新 PROGRESS.md，继续执行簇 D。

簇 D：页面帮助抽屉——前端
目标：每个主要页面右上角添加"?"按钮，点开显示页面说明，内容来自 help.js。

页面说明内容（追加到 frontend/src/constants/help.js）
javascript
export const PAGE_TIPS = {
  quote_list: {
    title: "报价单",
    description: "管理向客户提供的报价，报价被接受后可一键转换为合同。",
    tips: [
      "报价状态流转：草稿 → 已发送 → 已接受/已拒绝",
      "只有「已接受」状态的报价单才能转换为合同",
      "报价超过有效期会自动标记为已过期",
      "已接受的报价单核心字段不可修改，只能改备注",
    ],
  },
  project_detail: {
    title: "项目详情",
    description: "跟踪单个项目的执行状态、里程碑、工时和变更情况。",
    tips: [
      "项目关联报价被接受后，需求自动冻结，变更须通过变更单提交",
      "里程碑完成后才能触发对应收款流转",
      "关闭项目前需满足：所有里程碑完成、验收通过、款项到账、有交付物",
      "工时记录超出预计 20% 时，偏差备注为必填项",
    ],
  },
  contract_detail: {
    title: "合同详情",
    description: "查看合同信息、关联发票和收款记录。",
    tips: [
      "合同关联的所有发票金额累计不得超过合同总金额",
      "发票状态：草稿 → 已开票 → 客户已收 → 已核销",
      "已核销和已作废的发票不可删除",
    ],
  },
  finance_export: {
    title: "财务导出",
    description: "将业务数据导出为 Excel 文件，供代理记账使用。",
    tips: [
      "当前支持通用 Excel 格式，交代理记账导入其财务软件",
      "每次导出生成唯一批次号，可追溯导出记录",
      "导出失败时批次记录保留但无下载链接",
    ],
  },
  reconciliation: {
    title: "对账报表",
    description: "按自然月查看合同、收款、发票的汇总对账数据。",
    tips: [
      "期初余额 = 上月期末余额；系统第一期期初为 0",
      "「未对账记录」= 有合同但未关联发票的收款，需人工确认",
      "此报表为业务视角汇总，非会计准则下的财务报表",
    ],
  },
  consistency_check: {
    title: "数据核查",
    description: "检查合同、收款、发票三张表的数据是否一致。",
    tips: [
      "核查为只读操作，不会修改任何数据",
      "「未收款差异」= 合同金额 > 实收总额",
      "「核销差异」= 已核销发票总额与实收金额不一致",
      "差异金额低于 0.01 元不报告",
    ],
  },
  profit_view: {
    title: "利润分析",
    description: "查看项目的粗利润估算（业务视角，非会计利润）。",
    tips: [
      "收入基准为实收金额，不是合同金额",
      "工时成本 = 实际工时（小时）÷ 8 × 日费率",
      "无工时记录或无日费率时，工时成本显示为空",
      "固定成本和进项发票需手动关联到项目才计入",
    ],
  },
}
组件规范
新建 frontend/src/components/PageHelpDrawer.vue：

Props：pageKey: String
行为：
  从 PAGE_TIPS[pageKey] 读取内容（直接导入 help.js，不调用接口）
  无对应内容时不渲染"?"按钮，静默处理
  右上角渲染「? 帮助」按钮
  点击后从右侧滑出抽屉（桌面 320px，移动端全屏）
  内容：页面标题 + 描述 + 提示列表（≤5条）
  点击遮罩或关闭按钮收起
必须覆盖的页面
quote_list / project_detail / contract_detail
finance_export / reconciliation / consistency_check / profit_view
验收标准
所有必须页面均有"? 帮助"按钮
抽屉内容不超过 5 条
移动端全屏展示，有明显关闭按钮
无对应内容时不渲染按钮，不报错
无任何接口调用（纯静态）
运行全量测试，要求 0 FAILED。更新 PROGRESS.md，继续执行簇 E。

簇 E：错误展示增强——前端
目标：接口报错时，前端解析 help 字段并展示原因和下一步建议。

实现规范
新建 frontend/src/components/ErrorHelp.vue，修改全局 axios 拦截器（不新建拦截器）：

javascript
// 修改现有 axios 响应拦截器：

// HTTP 4xx 无 help 字段 → 维持现有 ElMessage.error(detail)，不变
// HTTP 4xx 有 help 字段 → 显示 ErrorHelp 对话框：
//   标题："操作失败"
//   原因：help.reason（一段话）
//   下一步（有序列表，1/2/3...）：help.next_steps
//   关闭按钮
// HTTP 5xx → ElMessage.error("服务器错误，请稍后重试")，不显示 help
// 网络错误 → ElMessage.error("网络连接失败，请检查网络")

// 全局拦截器统一处理，各页面无需重复实现
必须验证的错误场景
REQUIREMENT_FROZEN           → 对话框显示变更单引导步骤
MILESTONE_NOT_COMPLETED      → 对话框显示里程碑完成步骤
PROJECT_CLOSE_CONDITIONS_NOT_MET → 对话框显示关闭条件核查步骤
INVOICE_AMOUNT_EXCEEDS_CONTRACT  → 对话框显示已开票金额查看入口
QUOTE_ALREADY_CONVERTED      → 对话框显示已关联合同入口
验收标准
有 help 字段的 4xx 错误显示 ErrorHelp 对话框（原因 + 有序步骤列表）
无 help 字段的 4xx 错误维持现有 ElMessage，不变
5xx 错误显示通用提示，不显示 help
对话框有明显关闭按钮，步骤有序号
全局拦截器统一处理，无页面级重复代码
运行全量测试，要求 0 FAILED。更新 PROGRESS.md，继续执行簇 F。

簇 F：全局重构
目标：在全量测试通过的前提下验证帮助系统的整洁性。

必须验证的项目
1. 帮助内容零数据库依赖验证

确认无 help 相关数据库表存在
确认无 GET /help/* 路由存在
2. 静态文件集中管理验证

backend/core/help_content.py 为唯一后端帮助内容来源
frontend/src/constants/help.js 为唯一前端帮助内容来源
路由/视图层无帮助文字硬编码
3. 组件复用性验证

新增一个字段提示：只需在 help.js 的 FIELD_TIPS 中添加一行，不改组件
新增一个页面说明：只需在 help.js 的 PAGE_TIPS 中添加一个 key，不改组件
4. 错误码与帮助映射同步约束（强制，持续生效）

检查 error_codes.py 中的每一个错误码
若某错误码会返回给前端，必须评审是否需要 help 映射
未在 HELP_CONTENT 中的错误码须在代码注释中注明原因（如"# detail 已足够说明"）
后续版本新增错误码时，必须同步评审 help 映射，否则视为该错误码未完成
5. 现有测试不被破坏验证

v1.0 ~ v1.9 所有对 detail / code 字段的断言仍然通过
help 字段为可选追加，不影响现有断言
6. 最终全量回归

bash
pytest tests/ -v --tb=short
# 要求：0 FAILED
# 将完整输出写入 PROGRESS.md
四、完成标准
以下全部满足时，在 PROGRESS.md 写入"v1.10 执行完成"并记录时间：

 簇 A ~ F 全部状态为 ✅
 pytest tests/ -v 结果：0 FAILED，输出已记录在 PROGRESS.md
 backend/core/help_content.py 包含全部10个错误码帮助映射
 frontend/src/constants/help.js 包含字段提示、页面说明、流程步骤
 无 help 相关数据库表，无 GET /help/* 接口
 有 help 映射的错误码，接口报错时响应包含 help 字段
 detail / code 字段内容未被修改（有测试覆盖）
 FieldTip 组件覆盖所有必须字段
 PageHelpDrawer 组件覆盖所有必须页面
 流程图在导航栏和首页均有入口
 ErrorHelp 对话框在5个指定场景正确触发
 logs/ 目录本次执行产生了日志文件
附录 A：帮助系统设计原则
1. 不打扰：正常操作时不出现任何帮助弹窗
2. 按需出现：用户主动点击或操作出错时才展示
3. 可执行：帮助内容必须包含"下一步做什么"，不只是描述问题
4. 零数据库：内容集中在两个静态文件，随代码版本管理
5. 可降级：无对应内容时静默处理，不影响主业务
附录 B：错误码帮助覆盖清单
错误码	模块	有帮助内容
REQUIREMENT_FROZEN	v1.7	✅
CHANGE_ORDER_INVALID_TRANSITION	v1.7	✅
MILESTONE_NOT_COMPLETED	v1.7	✅
PROJECT_CLOSE_CONDITIONS_NOT_MET	v1.7	✅
PROJECT_ALREADY_CLOSED	v1.7	✅
INVOICE_AMOUNT_EXCEEDS_CONTRACT	v1.8	✅
INVOICE_STATUS_INVALID_TRANSITION	v1.8	✅
INVOICE_CANNOT_DELETE	v1.8	✅
QUOTE_ALREADY_CONVERTED	v1.6	✅
QUOTE_NOT_ACCEPTED	v1.6	✅
PAYMENT_STATUS_INVALID_TRANSITION	v1.7	❌ 通用提示已够用
QUOTE_STATUS_INVALID_TRANSITION	v1.6	❌ 通用提示已够用
NOT_FOUND	通用	❌ detail 已说明
VALIDATION_ERROR	通用	❌ detail 已说明
附录 C：核心概念定义（可选增强，不计入 v1.10 完成标准）
性质：概念说明层（"这是什么"），与 v1.10 帮助与引导模块（"该怎么做"）职责不同。不影响 v1.10 完成标准，可在完成后单独实施。

实施方式：

后端：作为 docstring 写入对应 Model 类，实施时顺手加，不单独立项
前端：在流程图页面新增"核心概念"Tab，与 WORKFLOW_STEPS 放同一入口
不做页面帮助抽屉，不做操作引导，只做"这是什么"的定义说明
C.1 核心模型定义（写入后端 Model docstring）
python
class Customer:
    """
    客户：合同的签约主体（公司或个人）。
    用于合同、收款、发票、报价单的关联归属。
    一个客户可对应多份合同和多个项目。
    注意：Customer 代表签约法人主体，不是具体联系人。
    """

class Contract:
    """
    合同：与客户签订的正式服务协议。
    合同金额是收款和发票的核对基准，不等同于实收金额。
    一份合同通常对应一个项目（1:1），特殊情况可扩展。
    合同是发票开具的前提，发票必须关联已存在的合同。
    """

class Project:
    """
    项目：合同的执行单元，用于跟踪交付进度和工时成本。
    通常一份合同对应一个项目。
    项目是里程碑、工时记录、变更单、验收、交付物的归属容器。
    项目粗利润 = 实收金额 - 工时成本 - 固定成本 - 进项成本。
    """

class Milestone:
    """
    里程碑：项目中的阶段性交付节点，绑定收款金额。
    里程碑完成（status=completed）才能触发开票和收款流转。
    里程碑是项目关闭条件的核心依据之一。
    """

class Requirement:
    """
    需求：项目的功能需求记录。
    报价单被客户接受后，需求自动进入冻结状态。
    冻结后的需求变更必须通过变更单（ChangeOrder）流转，不可直接修改。
    requirement_changes 表记录字段级变更历史快照（内部审计用）。
    """

class FinanceRecord:
    """
    收款记录：实际到账的款项记录，关联合同和发票。
    finance_records.amount 为实收金额，是粗利润计算的收入基准。
    收款记录可关联发票（invoice_id），也可不关联（未开票收款）。
    reconciliation_status 表示该条收款的对账确认状态，由批量同步接口驱动。
    """
C.2 核心关系说明（写入前端 help.js CONCEPTS 常量）
javascript
// 追加到 frontend/src/constants/help.js

export const CORE_CONCEPTS = [
  {
    term: "客户（Customer）",
    definition: "合同的签约主体（公司或个人），不是具体联系人。一个客户可对应多份合同和多个项目。",
    related: ["合同", "报价单", "发票", "收款"],
  },
  {
    term: "合同（Contract）",
    definition: "与客户签订的正式服务协议。合同金额是收款和发票的核对基准，不等于实收金额。",
    related: ["发票", "收款", "项目"],
    key_rule: "所有发票金额累计不得超过合同金额",
  },
  {
    term: "项目（Project）",
    definition: "合同的执行单元。通常一份合同对应一个项目。是里程碑、工时、变更单、验收的归属容器。",
    related: ["合同", "里程碑", "工时记录", "变更单"],
    key_rule: "项目关闭需满足：里程碑全完成、验收通过、款项到账、有交付物",
  },
  {
    term: "里程碑（Milestone）",
    definition: "项目中的阶段性交付节点，必须绑定收款金额。完成后才能触发开票和收款流转。",
    related: ["项目", "收款"],
    key_rule: "里程碑未完成不可触发开票，完成后才可标记收款状态",
  },
  {
    term: "需求（Requirement）",
    definition: "项目的功能需求记录。报价被客户接受后自动冻结，冻结后变更须走变更单流程。",
    related: ["项目", "报价单", "变更单"],
    key_rule: "冻结后直接修改需求会报错 REQUIREMENT_FROZEN",
  },
  {
    term: "收款记录（FinanceRecord）",
    definition: "实际到账的款项。是粗利润计算的收入基准（不用合同金额）。可关联发票，也可不关联。",
    related: ["合同", "发票", "项目"],
    key_rule: "无合同关联的收款记录会出现在数据核查的「未对账记录」中",
  },
  {
    term: "销项发票（Invoice）",
    definition: "你开给客户的发票。必须关联合同，累计金额不超合同总额。已核销（verified）为终态。",
    related: ["合同", "收款"],
    key_rule: "invoices.status=verified 是「已核销」的唯一来源，用于数据一致性校验",
  },
  {
    term: "变更单（ChangeOrder）",
    definition: "需求冻结后，新增或修改需求的正式入口。需客户确认（confirmed）才能进入开发。",
    related: ["需求", "项目"],
    key_rule: "confirmed 的变更单才允许关联里程碑或纳入开发",
  },
]
C.3 前端展示要求
在流程图页面（簇 B 的流程图入口）新增"核心概念"Tab：

流程图页面结构：
  Tab 1：业务流程（WORKFLOW_STEPS，已在簇 B 实现）
  Tab 2：核心概念（CORE_CONCEPTS，本附录新增）

核心概念 Tab 展示：
  卡片列表，每张卡片展示：
    - 术语名称（加粗）
    - 定义说明（一段话）
    - 关键规则（若有，用黄色标注）
    - 相关概念标签（点击跳转到对应卡片）
  无搜索框，无分类筛选，全部平铺展示（共8个概念）
  无接口调用（纯静态）
C.4 实施说明
后端 docstring：在簇 A 实施时顺手写入各 Model 类，不单独立簇，不计入测试
前端概念 Tab：在簇 B 实施流程图时同步完成，共用同一页面入口
工作量估计：后端 docstring ≈ 30分钟，前端 Tab ≈ 2小时
总计：不超过半天，不需要新增簇或调整 PROGRESS.md
