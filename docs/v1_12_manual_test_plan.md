# v1.12 手动测试计划

覆盖 PRD 全部要求，按簇分组。每项测试需记录 PASS/FAIL。

---

## 簇 A：数据库迁移

| # | 测试项 | 操作步骤 | 预期结果 | 状态 |
|---|--------|----------|----------|------|
| A1 | templates 表存在 | `sqlite3 shubiao.db ".schema templates"` | 表结构匹配 PRD SQL | |
| A2 | quotations 新字段 | 查看 quotations 表结构 | generated_content, template_id, content_generated_at 存在 | |
| A3 | contracts 新字段 | 查看 contracts 表结构 | generated_content, template_id, content_generated_at 存在 | |
| A4 | 默认模板存在 | `SELECT * FROM templates WHERE is_default=1` | 2 条记录（quotation + contract） | |
| A5 | 唯一约束生效 | 尝试插入第二个 quotation 默认模板 | 唯一约束冲突 | |

---

## 簇 B：模板管理——后端 + 前端

| # | 测试项 | 操作步骤 | 预期结果 | 状态 |
|---|--------|----------|----------|------|
| B1 | 模板列表 | 登录 → 侧边栏「模板管理」 | 显示 2 个默认模板，带"默认"标签 | |
| B2 | 新建模板 | 点击「新建模板」→ 填写名称、类型、内容（含 Jinja2 变量）→ 确定 | 成功创建，列表新增 1 条 | |
| B3 | 无效 Jinja2 被拒绝 | 新建模板，内容填 `{% if x %}不完整` → 确定 | 400 错误，提示模板语法错误 | |
| B4 | 编辑模板 | 点击编辑 → 修改名称/内容 → 确定 | 更新成功 | |
| B5 | 设为默认 | 点击非默认模板的「设为默认」→ 确认 | 新模板变默认，旧默认取消 | |
| B6 | 默认模板不可删 | 默认模板行无删除按钮，显示"默认不可删" | 删除按钮禁用 | |
| B7 | 非默认可删除 | 点击非默认模板「删除」→ 确认"删除后已生成内容不受影响" | 删除成功 | |
| B8 | 变量提示 | 新建/编辑时右侧显示变量提示 | 必填/可选分组显示，变量名与 PRD 附录 A 一致 | |
| B9 | 非法类型被拒绝 | 新建模板时通过 API 传 template_type=invalid | 422 错误 | |

---

## 簇 C：报价单生成——后端 + 前端

| # | 测试项 | 操作步骤 | 预期结果 | 状态 |
|---|--------|----------|----------|------|
| C1 | 项目一键生成报价 | 进入有客户关联的项目详情 → 点击「一键生成报价」 | 创建成功，提示"报价草稿已创建" | |
| C2 | 草稿去重 | 同一项目再次点击「一键生成报价」 | 弹出确认框"已有生成报价草稿（编号 BJ-xxx），是否跳转查看？" | |
| C3 | 草稿去重跳转 | 确认框点击"确定" | 跳转到报价单列表并打开对应详情 | |
| C4 | 报价单生成内容 | 报价单详情页（draft 状态）→ 点击「生成内容」 | 生成成功，内容展示区显示渲染后的文本 | |
| C5 | 覆盖确认 | 已有生成内容后再次点击「生成内容」 | 弹出"已有生成内容（含手工修改），确认覆盖？" | |
| C6 | 覆盖取消 | 覆盖确认框点"取消" | 不调用接口，内容不变 | |
| C7 | 内容展示格式 | 查看生成的内容 | white-space: pre-wrap 保留换行和缩进 | |
| C8 | 手工编辑 | 点击「手工编辑」→ 修改内容 → 保存 | 内容更新，content_generated_at 不更新 | |
| C9 | 预览 | 点击「预览」 | 弹窗显示渲染内容，不写库 | |
| C10 | accepted 冻结 | 将报价单状态改为 accepted → 不显示「生成内容」按钮 | 显示"内容已冻结" | |
| C11 | accepted 不可编辑 | accepted 状态下点击「手工编辑」 | 400 错误，CONTENT_FROZEN | |
| C12 | accepted 可预览 | accepted 状态下点击「预览」 | 正常显示预览内容 | |

---

## 簇 D：合同生成——后端 + 前端

| # | 测试项 | 操作步骤 | 预期结果 | 状态 |
|---|--------|----------|----------|------|
| D1 | 报价转合同 | accepted 报价单详情页 → 点击「转合同并生成草稿」→ 确认 | 创建合同草稿，自动填充内容 | |
| D2 | 已转合同拒绝 | 同一报价单再次点击「转合同」 | 409 错误，提示已转成合同 | |
| D3 | 合同生成内容 | 合同详情页（draft 状态）→ 点击「生成内容」 | 生成成功 | |
| D4 | 合同覆盖确认 | 已有内容后再次生成 | 弹出覆盖确认 | |
| D5 | 合同手工编辑 | 点击「手工编辑」→ 修改 → 保存 | 内容更新，content_generated_at 不更新 | |
| D6 | 合同预览 | 点击「预览」 | 正常显示，不写库 | |
| D7 | active 冻结 | 将合同状态改为 active → 不显示生成按钮 | 显示"内容已冻结" | |
| D8 | active 不可编辑 | active 状态下手工编辑 | 400 错误，CONTENT_FROZEN | |
| D9 | 无报价单编号 | 手动创建的合同（无 quotation_id）→ 生成内容 | 422 错误，QUOTE_NO_QUOTATION_ID | |
| D10 | 合同包含 quotation_no | 查看生成的合同内容 | 内容中包含关联报价单的编号 | |

---

## 簇 E：前端联调

| # | 测试项 | 操作步骤 | 预期结果 | 状态 |
|---|--------|----------|----------|------|
| E1 | 路由正确 | 访问 /settings/templates | 正常加载模板管理页 | |
| E2 | 侧边栏链接 | 点击侧边栏「模板管理」 | 跳转到 /settings/templates | |
| E3 | 内容格式保留 | 生成内容中包含换行、缩进 | 展示时保留原始格式 | |
| E4 | 错误帮助 | 触发 TEMPLATE_NOT_FOUND 错误 | 响应中包含 help 字段 | |

---

## 簇 F：全局重构

| # | 测试项 | 操作步骤 | 预期结果 | 状态 |
|---|--------|----------|----------|------|
| F1 | 变量对齐 | 运行 `pytest tests/test_template_vars_alignment.py -v` | 2 passed | |
| F2 | 全量回归 | 运行 `pytest tests/ -v --tb=short` | 0 NEW failures | |
| F3 | 默认模板渲染 | 使用默认报价模板生成内容 | 渲染成功，不报 TEMPLATE_RENDER_FAILED | |
| F4 | 默认合同渲染 | 使用默认合同模板生成内容 | 渲染成功，不报 TEMPLATE_RENDER_FAILED | |
